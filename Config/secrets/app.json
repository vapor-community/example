{
	// Replaces values from all other environment files.
	// JSON files in secrets/* should be gitignored since
	// they might contain secret information that should 
	// stay local to the machine hosting the project.
	"key": "secret-key"
}